# Simple-Linear-Regression - Experiments

This folder is dedicated to some simple experiments in order to comprove that the implementation of each element of simple linear regression is working fine.
So, the treatment over the data and the selected dataset are kind of simple, but effective.
As the datasets selected are small, we are using only cross-validation instead of a validation set.

